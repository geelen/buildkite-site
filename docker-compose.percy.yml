version: '3'
services:
  percy:
    build:
      context: .
      dockerfile: Dockerfile.percy
    environment:
      - PERCY_TOKEN
      - PERCY_PROJECT
    volumes:
      - ".git:/site/.git" # Percy needs to be able to find the git commit