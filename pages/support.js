import Page, { page } from 'components/Page'

export default page(({ loggedIn }) => (
  <Page
    headTitle="Buildkite Support"
    title="Support"
    description="TODO"
    loggedIn={loggedIn}
  >
  </Page>
))