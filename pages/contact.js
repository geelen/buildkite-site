import Page, { page } from 'components/Page'

export default page(({ loggedIn }) => (
  <Page
    headTitle="Contact Buildkite"
    title="Contact Us"
    description="TODO"
    loggedIn={loggedIn}
    >
  </Page>
))